{% extends 'base.html.twig' %}

{% block title %}AssocierCategorie{% endblock %}

{% block body %}
    <h1>AssocierCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ associer_categorie.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('associer_categorie_index') }}">back to list</a>

    <a href="{{ path('associer_categorie_edit', {'id': associer_categorie.id}) }}">edit</a>

    {{ include('associer_categorie/_delete_form.html.twig') }}
{% endblock %}
