{% extends 'base.html.twig' %}

{% block title %}Resider{% endblock %}

{% block body %}
    <h1>Resider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resider.id }}</td>
            </tr>
            <tr>
                <th>Defaut</th>
                <td>{{ resider.defaut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resider_index', { 'utilisateur': utilisateur.id }) }}">back to list</a>

    <a href="{{ path('resider_edit', {'utilisateur': utilisateur.id, 'id': resider.id}) }}">edit</a>

    {{ include('resider/_delete_form.html.twig') }}
{% endblock %}
