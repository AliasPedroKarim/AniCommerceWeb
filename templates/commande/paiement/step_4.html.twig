{% extends 'base.html.twig' %}

{% block title %}Paiment Etape 4 - {{ parent() }}{% endblock %}

{% block body %}

	<div class="container">
		<div class="py-5 text-center">
			<img class="d-block mx-auto mb-4" src="{{ asset('favicon.gif') }}" alt="" width="72" height="72">
			<h2>Commande sur {{ app_name }}</h2>
			<p class="lead">
				Votre commande à bien été prise en compte.
			</p>
		</div>

		<div id="ButtonPayment" class="row d-flex justify-content-center">
			<a href="{{ path('ligne_commande_index') }}" class="btn btn-primary w-25">Acceuil</a>
		</div>

	</div>

{% endblock %}