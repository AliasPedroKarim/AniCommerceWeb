{% extends 'base.html.twig' %}

{% block title %}TypeReduction{% endblock %}

{% block body %}
    <h1>TypeReduction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_reduction.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_reduction.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_reduction_index') }}">back to list</a>

    <a href="{{ path('type_reduction_edit', {'id': type_reduction.id}) }}">edit</a>

    {{ include('type_reduction/_delete_form.html.twig') }}
{% endblock %}
